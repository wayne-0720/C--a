#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    while (n != 0)
    {
        cout << "I don't say swear words!\n";
        n--;
    }
    return 0;
}
