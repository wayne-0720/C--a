#include<bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int c;
    while (cin>>c)
    {
        vector<int> v;
        for (int i = 0; i < c;i++)
        {
            
        }
    }
    return 0;
}
